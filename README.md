# hackrpi-2025 (parent repo)

This repository contains two git submodules (local):

- `frontend` — frontend submodule (renamed from `submodule-a`)
- `backend` — backend submodule (renamed from `submodule-b`)

They are added as local submodules for demonstration.
